// math 모듈
// 두 숫자의 합을 반환하는 함수
export function add(a, b) {
    return a + b;
}

// 두 숫자의 차를 반환하는 함수
export function sub(a, b) {
    return a - b;
}

// export { add, sub };

export default function multiply(a, b) {
    return a * b;
}